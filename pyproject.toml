[project]
name = "system-design-decision-cookbook"
version = "0.1.0"
description = "Docs tooling for the System Design Decision Cookbook."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.6,<10",
]

[tool.uv]
package = false
