# Stream

This page is the component-page target for stream decisions. The detailed
decision tree is planned by the stream ticket; until then, use the
[component selection map](./) to decide whether retained event history is
justified.

Planned scope: event history, multiple consumers, ordering, retention, replay,
consumer lag, event-driven pipelines, and operational checklist items.
