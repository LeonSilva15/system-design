# Service Layer

This page is the component-page target for service layer decisions. The detailed
decision tree is planned by the service layer ticket; until then, use the
[component selection map](./) to decide whether a service boundary is justified.

Planned scope: modular monoliths, microservices, domain boundaries, ownership,
deployment independence, and scaling boundaries.
