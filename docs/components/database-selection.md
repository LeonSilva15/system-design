# Database Selection

This page is the component-page target for database selection decisions. The
detailed decision tree is planned by the database selection ticket; until then,
use the [component selection map](./) to decide when durable storage is
justified.

Planned scope: relational, document, key-value, graph, time-series, object
storage, analytical stores, defaults, and trade-offs.
