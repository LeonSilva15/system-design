# Object Storage

This page is the component-page target for object storage decisions. The
detailed decision tree is planned by the object storage ticket; until then, use
the [component selection map](./) to decide whether blobs should leave the
primary database path.

Planned scope: files, images, videos, backups, signed URLs, metadata, multipart
upload, lifecycle rules, processing, and checklist items.
