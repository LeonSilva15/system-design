# Cache

This page is the component-page target for caching decisions. The detailed
decision tree is planned by the cache ticket; until then, use the
[component selection map](./) to decide whether read pressure justifies a cache.

Planned scope: read-heavy systems, expensive reads, stale data, invalidation,
hot keys, TTLs, fallback behavior, and common mistakes.
