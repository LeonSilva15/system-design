# CDN

This page is the component-page target for CDN decisions. The detailed decision
tree is planned by the CDN ticket; until then, use the
[component selection map](./) to decide whether edge delivery is justified.

Planned scope: static content, global users, cacheability, invalidation, signed
URLs, origin protection, and checklist items.
