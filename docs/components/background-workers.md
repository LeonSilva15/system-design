# Background Workers

This page is the component-page target for background worker decisions. The
detailed decision tree is planned by the background workers ticket; until then,
use the [component selection map](./) to decide whether work should move outside
the user response path.

Planned scope: delayed work, CPU-heavy work, retryable tasks, scheduled jobs,
worker pools, job visibility, and checklist items.
