# Load Balancer

This page is the component-page target for load balancer decisions. The detailed
decision tree is planned by the load balancer ticket; until then, use the
[component selection map](./) to decide whether routing across instances is
justified.

Planned scope: stateless services, health checks, routing, failover, sticky
sessions, regional traffic, and checklist items.
