# Search Index

This page is the component-page target for search index decisions. The detailed
decision tree is planned by the search index ticket; until then, use the
[component selection map](./) to decide whether user-facing retrieval needs more
than database filters.

Planned scope: full-text search, ranking, filters, autocomplete, typo tolerance,
reindexing, freshness, and checklist items.
