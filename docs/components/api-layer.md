# API Layer

This page is the component-page target for API layer decisions. The detailed
decision tree is planned by the API layer ticket; until then, use the
[component selection map](./) to decide whether an API boundary is justified.

Planned scope: REST, gRPC, WebSockets, SSE, polling, API gateways,
authentication, validation, versioning, and rate limits.
