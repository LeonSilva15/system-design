# Queue

This page is the component-page target for queue decisions. The detailed
decision tree is planned by the queue ticket; until then, use the
[component selection map](./) to decide whether delayed task delivery is
justified.

Planned scope: async work, retries, delay tolerance, ordering, backpressure,
dead-letter queues, idempotency, and the complexity queues add.
